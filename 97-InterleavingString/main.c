#include <stdio.h>
#include <stdbool.h>
#include <string.h>

bool isInterleave(char* s1, char* s2, char* s3) {
    int nums3=strlen(s3);
    int i=0;
    while (s3[i]!='\0'){

    }
}
int main() {
    printf("Hello, World!\n");
    return 0;
}