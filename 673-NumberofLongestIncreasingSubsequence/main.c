#include <stdio.h>

int findNumberOfLIS(int* nums, int numsSize) {
    int i,j;
    for(i=0;i<numsSize;i++){

    }
}
int main() {
    int num[5]={1,3,5,4,7};
    int max;
    max=findNumberOfLIS(num,5);
    printf("%d",max);
    return 0;
}